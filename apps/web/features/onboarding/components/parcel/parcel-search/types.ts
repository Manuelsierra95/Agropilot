export type {
  AddressQuery,
  ParcelAddress,
  ParcelSearchResult,
  SearchParcelFn,
  SearchParcelInput,
} from "@/lib/cadastre/types"

export type ParcelSearchMode = "coords" | "refcat" | "address"
