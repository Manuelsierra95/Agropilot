export type {
  AddressQuery,
  ParcelAddress,
  ParcelSearchResult,
  SearchParcelFn,
  SearchParcelInput,
} from "@workspace/web/lib/cadastre/types"

export type ParcelSearchMode = "coords" | "refcat" | "address"
